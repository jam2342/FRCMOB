# Grouped rating pipeline modules.
#
# The rating system now lives under ``app.services.ratings`` while the
# legacy ``app.services.rating_*`` modules remain as compatibility
# wrappers to avoid breaking imports.
